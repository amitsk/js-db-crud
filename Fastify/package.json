{
  "name": "fastify-crud-api",
  "version": "1.0.0",
  "description": "Fastify API for CRUD operations on users, products, and orders",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "format": "biome format",
    "lint": "biome lint",
    "check": "biome check",
    "db:generate": "drizzle-kit generate",
    "db:migrate": "drizzle-kit migrate",
    "db:push": "drizzle-kit push",
    "db:studio": "drizzle-kit studio"
  },
  "dependencies": {
    "@fastify/compress": "^8.3.0",
    "@fastify/cors": "^10.1.0",
    "@fastify/helmet": "^13.0.2",
    "dotenv": "^16.4.5",
    "drizzle-orm": "^0.39.0",
    "fastify": "^5.0.0",
    "pg": "^8.11.0",
    "zod": "^4.1.11"
  },
  "devDependencies": {
    "@biomejs/biome": "2.2.4",
    "drizzle-kit": "^0.30.0"
  }
}
